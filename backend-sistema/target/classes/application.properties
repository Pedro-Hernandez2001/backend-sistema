# Configuración del servidor
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/Restaurante?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate - NO CREAR TABLAS, usar las existentes
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuración JWT
app.jwt.secret=tuClaveSecretaMuySeguraParaJWTQueDebesChangeEnProduccion
app.jwt.expiration=86400000

# Configuración de logging
logging.level.com.example=DEBUG
logging.level.org.springframework.security=DEBUG

# Configuración de DevTools
spring.devtools.restart.enabled=true